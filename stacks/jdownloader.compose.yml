---
services:
  app:
    container_name: jdownloader
    image: jlesage/jdownloader-2:latest
    restart: unless-stopped
    ports:
      - ${HOST_PORT_HTTP}:${CONTAINER_PORT_HTTP}
    volumes:
      - $HOST_VOLUME_PATH/downloads:/output
