---
services:
  dash:
    container_name: dash
    image: mauricenino/dashdot:latest
    restart: unless-stopped
    privileged: true
    volumes:
      - /:/mnt/host:ro
networks:
  default:
    external: true
    name: nginx-proxy-manager
